// FileMetadataEditor is removed — the underlying API endpoints no longer exist.
import React from 'react';

interface FileMetadataEditorProps {
  [key: string]: any;
}

const FileMetadataEditor: React.FC<FileMetadataEditorProps> = () => null;

export default FileMetadataEditor;
